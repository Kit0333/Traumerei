#ifndef GAMESTATE_HPP
#define GAMESTATE_HPP

enum class GameState{PLAY,EXIT};

// There is two state of the game, PLAY when the game is still processering and EXIT when the game is about to close.

#endif